DATABASE_URL=""

# NextAuth Configuration
NEXTAUTH_SECRET="" # Used for JWT signing and encryption
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

BLOB_READ_WRITE_TOKEN=""

SUPER_ADMIN_EMAIL="your_super@example.com"
SUPER_ADMIN_PASSWORD="superStrongP@sswordHere!"

# Session Configuration (in minutes)
SESSION_MAX_AGE="15"     # 15 minutes - Standard session duration
JWT_MAX_AGE="15"        # 15 minutes - Matches session duration

# Refresh Token Configuration (in minutes)
REFRESH_TOKEN_MAX_AGE="10080"     # 7 days - Standard refresh token duration
REFRESH_TOKEN_EXPIRY_THRESHOLD="1440" # 24 hours - Rotate tokens a day before expiry

# Rate Limiting
MAX_CONCURRENT_SESSIONS="5"         # Maximum number of concurrent sessions per user
REFRESH_TOKEN_RATE_LIMIT="10"       # Maximum refresh attempts per hour
REFRESH_TOKEN_RATE_WINDOW="60"      # Rate limit window in minutes (1 hour)

# Email (Optional)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM=""
